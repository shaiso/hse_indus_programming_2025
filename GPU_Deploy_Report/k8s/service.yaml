apiVersion: v1
kind: Service
metadata:
  name: resnet-service
  namespace: ml-production
spec:
  selector:
    app: resnet-inference
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
